This is the parent project for all other projects in the Skork Framework.

-- When creating a new project that isn't a Test project, add it to SkorkAPI.sln as a reference.

-- Continous integration only builds the SkorkEngine project.